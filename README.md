# Felipe's Arch Linux Dotfiles

---
2. Zathura:
	- The zathurarc file needs to be copied inside `/home/user/.config/zathura/`
	- Use the following command to install icons inside ranger
```
git clone https://github.com/alexanderjeurissen/ranger_devicons ~/.config/ranger/plugins/ranger_devicons
```

3. ranger File Manager:
	- The rc.conf file needs to be copied in `/home/user/.config/ranger`
	- In order to preview images in ranger use the following  `sudo pacman -Sy ueberzug`

