Instructions to install the font:

---

1. Create the following path:

(If not created)

TIP: You can also create a otf folder
```
mkdir -p /usr/local/share/fonts/ttf/NewFontName
```

2. In this example copy the files (except this install file)

cp ./ /usr/local/share/font/ttf/NewFontName

3. Run `fc-cache`

Apply this to every font
